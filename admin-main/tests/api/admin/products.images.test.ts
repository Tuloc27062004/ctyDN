import { describe, it } from "vitest";

describe("/api/admin/products/[id]/images", () => {
  it.todo("GET product images if supported");
  it.todo("POST add image");
  it.todo("DELETE image if supported");
});
